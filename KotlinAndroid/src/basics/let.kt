package basics

fun main(args: Array<String>) {

    /*
    File("example.txt").bufferedReader().Let {
        if (it.ready()) {
            println(it.readLine())
        }

    }

    val str: String? = "Kotlin"
    str?.Let {
        if (str.isNotEmpty()) {
            str.toLowerCase()
        }
    }
*/
}